# This file is part of the Sylius sandbox application.
# (c) Paweł Jędrzejewski

sandbox_blogger_post_list:
    pattern: /blog
    defaults: { _controller: SandboxBloggerBundle:Frontend\Post:list }

sandbox_blogger_post_show:
    pattern: /blog/{slug}
    defaults: { _controller: SandboxBloggerBundle:Frontend\Post:show }
